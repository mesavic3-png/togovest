import {ArrowRight,Bath,BedDouble,Heart,MapPin,Maximize} from "lucide-react";import Image from "next/image";import {properties} from "@/data/properties";
export function FeaturedProperties(){return <section id="biens" className="py-20 sm:py-28"><div className="shell"><div className="mb-10 flex flex-col justify-between gap-5 sm:mb-14 sm:flex-row sm:items-end"><div><p className="eyebrow">Sélection du moment</p><h2 className="mt-3 max-w-xl text-3xl font-extrabold tracking-tight sm:text-5xl">Des biens qui méritent votre attention.</h2></div><a href="#" className="flex items-center gap-2 font-bold text-forest">Voir tous les biens <ArrowRight size={19}/></a></div><div className="grid gap-6 lg:grid-cols-3">{properties.map(p=><article key={p.id} className="group overflow-hidden rounded-[1.75rem] bg-white shadow-[0_12px_50px_rgba(16,40,31,.08)]"><div className="relative h-72 overflow-hidden"><Image src={p.image} alt={p.title} fill className="object-cover transition duration-700 group-hover:scale-105"/><span className="absolute left-4 top-4 rounded-full bg-white px-3 py-1.5 text-xs font-bold">{p.type}</span><button className="absolute right-4 top-4 grid h-10 w-10 place-items-center rounded-full bg-white" aria-label="Ajouter aux favoris"><Heart size={18}/></button></div><div className="p-6"><p className="flex items-center gap-1.5 text-sm text-ink/55"><MapPin size={15}/>{p.location}</p><h3 className="mt-3 text-xl font-extrabold">{p.title}</h3><p className="mt-4 text-lg font-extrabold text-forest">{p.price}</p><div className="mt-5 flex gap-5 border-t border-ink/10 pt-5 text-sm text-ink/60">{p.beds>0&&<span className="flex gap-1.5"><BedDouble size={17}/>{p.beds}</span>}{p.baths>0&&<span className="flex gap-1.5"><Bath size={17}/>{p.baths}</span>}<span className="flex gap-1.5"><Maximize size={16}/>{p.area} m²</span></div></div></article>)}</div></div></section>}
